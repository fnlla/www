name: CodeQL

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '12 2 * * 1'

jobs:
  analyze:
    uses: fnlla/.github/.github/workflows/reusable-codeql.yml@main
    with:
      languages: ''
